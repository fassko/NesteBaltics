platform :ios, '9.0'

target 'NesteBaltics' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  pod 'GoogleMaps'
  pod 'GooglePlaces'
  pod 'Fabric'
  pod 'Crashlytics'
  pod 'SwiftyJSON'

  target 'NesteBalticsTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'NesteBalticsUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
